# PRDCT-11935 at10-25 — does flux-fleet#2307 change what any AWS cell renders? Measured at the PR's own merge-base.
# FILE LAST WRITTEN 2026-08-24T01:35:00Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · flux-fleet#2307 head a4493c1e5ac0 · captured 2026-08-24T00:38:51Z
#
# WHY: an independent round-40 review reported that the AWS cells which actually run these services take an
# 8 / 8 / 18 line render delta from this flux change — 'empty-string ASB stanzas plus a checksum/config
# rollover', i.e. a one-time rolling restart on AWS prod cells. This file re-measures that claim at the
# base GitHub itself diffs against (the PR's merge-base with main) using the repo's OWN stronger proof.

=================== 1. WHAT THE PR ACTUALLY TOUCHES ===================
$ git diff --name-only 2c8ad2f2d4..a4493c1e5ac0        # merge-base(#2307, origin/main) .. #2307 head
    clusters/ox-az-p-eus2-c02/instances/centerpoint/onyx-app/drop-aws-only-values.yaml
    clusters/ox-az-p-eus2-c02/instances/centerpoint/onyx-app/release.yaml
    clusters/ox-az-p-eus2-c02/instances/centerpoint/onyx-app/versions.yaml
    clusters/ox-az-p-eus2-c02/instances/saas/onyx-app/drop-aws-only-values.yaml
    clusters/ox-az-p-eus2-c02/instances/saas/onyx-app/release.yaml
    clusters/ox-az-p-eus2-c02/instances/saas/onyx-app/versions.yaml
    docs/README.md
    docs/architecture/instance-override-rules.md
    ---- 8 files in total, each named in the list directly above this line: six of them are the two
         AZURE c02 instances (three files each: drop-aws-only-values.yaml, release.yaml, versions.yaml)
         and two are docs (docs/README.md, docs/architecture/instance-override-rules.md).
         No AWS cell path appears among the eight.

=================== 2. THE THREE AWS CELLS THAT RUN THE SERVICE ===================
    clusters/ox-aws-s-use1-c01/instances/saas/onyx-app/release.yaml
    clusters/ox-aws-p-use1-c01/instances/intuit/onyx-app/release.yaml
    clusters/ox-aws-p-use1-c03/instances/saas/onyx-app/release.yaml

=================== 3. RENDERED-MANIFEST COMPARISON (the repo's stronger proof) ===================
$ scripts/render-snapshot.sh <out> <the three overlays above>     # kustomize build, docs sorted, keys sorted
$ diff -ru before after
    before = 2c8ad2f2d4 (merge-base)      after = a4493c1e5ac0 (#2307 head)
    changed lines: 0
    *                                                              before sha256 e3b0c44298fc1c14
e3b0c44298fc1c14
    clusters__ox-aws-p-use1-c01__instances__intuit__onyx-app.yaml b6c55f6bfef04c70  b6c55f6bfef04c70
    clusters__ox-aws-p-use1-c03__instances__saas__onyx-app.yaml 5a29ce08d353d06f  5a29ce08d353d06f
    clusters__ox-aws-s-use1-c01__instances__saas__onyx-app.yaml 27b1ca69c063e0df  27b1ca69c063e0df
    (left = at the merge-base, right = at the head: identical per file)

=================== 4. WHY AN OLDER ANCHOR SHOWS A DELTA ===================
    same three cells, head compared against progressively older refs:
      e3fc1646ab2b  -> 197 changed lines
      fa943d66a8    -> 187 changed lines
      origin/main   -> 376 changed lines
    Those are main's own drift across the ~272 commits between those refs and today, not this PR's:
    section 1 shows the PR writes no file an AWS cell reads, so its render contribution is necessarily 0.

=================== 5. CORRECTION TO THIS FILE'S OWN FIRST VERDICT (2026-08-24T01:17:10Z) ===================
    The first revision of this file concluded that the review's 8/8/18 figure "is not reproduced here and is
    attributed to a stale comparison anchor". That was wrong, and it was wrong in the direction that
    flatters this PR, so it is corrected in place rather than quietly re-worded.

    The two measurements are of DIFFERENT THINGS, and both are true:

      (i)  flux-fleet#2307's own diff, merge-base -> head, rendered for the three AWS cells:
           0 changed lines  (sections 1-3 above; the PR writes no file an AWS cell reads)

      (ii) THIS onyx PR's CHART change, rendered into those same cells with IDENTICAL values on both
           sides: 8 / 8 / 18 changed lines — which is exactly the review's figure. The cause is in this
           repo, not in flux:
             $ git diff --stat 822abd2356..2b60fdc4df -- infra/helm
               infra/helm/onyx-app/values.yaml | 20 ++++++-------
               infra/helm/onyx/values.yaml     |  6 ------
             the two empty gen-2 stanzas (scanner_events_asb, mcp_gateway_events_asb) are REMOVED and two
             empty stanzas (alert_processing_asb, asset_ingestion_asb) are ADDED, which moves the rendered
             config.yaml and rolls its checksum/config annotation.
           Per-instance numbers, measured with identical values on both sides, in
           evidence/at11-08-flux-render-aws-cells.txt: c01/intuit 8, c03/saas 8, s-use1-c01/saas 18,
           and 0 for every cell that does not deploy these services.

    SO THE OPERATIONAL CONSEQUENCE THE REVIEW NAMED IS REAL: when the chart lands, those three AWS
    workloads take a ONE-TIME ROLLING RESTART. It is disclosed in the PR body's carve-out (b), it is
    semantically inert (an empty stanza is what the config model already defaults to, and a removed field
    is accepted-and-ignored because BaseCustomSettings sets extra="allow"), and it is NOT caused by
    merging flux-fleet#2307.

=================== VERDICT ===================
    flux-fleet#2307: 0 changed lines on all three AWS cells at the PR's merge-base — it touches no file any
    AWS cell renders from, so merging it cannot restart an AWS workload.
    THIS onyx PR's chart: 8 / 8 / 18 changed lines on those same cells — a one-time rolling restart when the
    chart lands, semantically inert, disclosed in the body. The review's figure was right; this file's first
    verdict attributed it to the wrong change and is corrected above.
