# PRDCT-11935 at3-02 — the message received FROM the real $DeadLetterQueue, with its deadLetterReason
# FILE LAST WRITTEN 2026-08-23T21:42:39Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · image localhost:5001/onyx/endpoint-asset-ingestion:r46b-head · captured 2026-08-23T21:40:51Z
#
# RE-DRIVEN FROM SCRATCH ON THE CURRENT SHIPPED HEAD (2b60fdc4df) together with at3-01/03/04. Read at3-01
# first: it carries the body, the send, the queue deltas and the code path. This file is the read-back.

=================== THE READ-BACK — a real RECEIVE, not only a peek ===================
    Taken from the queue's native $DeadLetterQueue sub-queue (Azure's own, not an application table),
    with a peek-LOCK receive that walked the whole sub-queue. NOTHING WAS SETTLED — no complete, no
    abandon, no dead-letter — so the sub-queue is left exactly as it was found and this run does not
    destroy evidence another round may need. Walking the whole sub-queue matters: the target was NOT in
    the first page, and a smaller read returning "0 matches" would have looked like a failure when it was
    only a paging limit.

    $ python3 /tmp/prdct11935/r50/dlqrecv.py e9803f4a-42ee-4a49-a2ee-9b2d2e409e43
    {
     "message_id": "e9803f4a-42ee-4a49-a2ee-9b2d2e409e43",
     "dead_letter_reason": "no ingestible object in the message",
     "dead_letter_error_description": null,
     "delivery_count": 0,
     "label_aka_subject": "/blobServices/default/containers/ingestion",
     "enqueued_time_utc": "2026-08-23 21:32:33.144000+00:00",
     "body_event_id_marker": "r50-poison-213225",
     "body_client_request_id_marker": "r50-poison-213225",
     "body_subject": "/blobServices/default/containers/ingestion",
     "body_blob_url": "https://prdct11935st.blob.core.windows.net/ingestion",
     "body_event_type": "Microsoft.Storage.BlobCreated"
    }
    # RECEIVED (peek-lock, nothing settled) 121 message(s) from endpoint-asset-ingestion-prdct11935/$DeadLetterQueue at 2026-08-23T21:33:45Z; matches for e9803f4a-42ee-4a49-a2ee-9b2d2e409e43: 1

=================== WHAT THIS PROVES, TERM BY TERM ===================
    · joined to at3-01 by message_id e9803f4a-42ee-4a49-a2ee-9b2d2e409e43 — the id the consumer's own
      "Dead-lettered message" line names (at3-03)
    · `dead_letter_reason` is the APPLICATION's own reason — "no ingestible object in the message", the
      exact string built at message_handler.py:107 — NOT the broker's "MaxDeliveryCountExceeded". So this
      is a deliberate park on the first look, not a retry budget that ran out.
    · `dead_letter_error_description` is null: nothing threw. The lane decided, it did not crash.
    · `delivery_count` 0 — the message was parked on its FIRST delivery, so it never looped. (This is the
      value at receive time; because the receive settled nothing, a later reader may see it higher.)
    · both markers carried inside the body — `id` and `data.clientRequestId` = r50-poison-213225 — match
      what at3-01 sent, and `body_blob_url` is still the container-only url that made it unprocessable.
    · `label_aka_subject` is the container-scoped subject the real Event Grid subscription stamps, so the
      message the DLQ holds is the message the subscription would have delivered.
    · 121 messages were read and exactly 1 matched: the message is identified by id, not by assuming an
      empty sub-queue.

=================== AND THE GUARD IT SETTLES ===================
    at3's guard is that the malformed message must NOT be silently acked and must NOT poison-loop.
    Silently-acked would mean it is NOWHERE — this read shows it IS in the $DeadLetterQueue, with a
    reason. Poison-looping would mean a rising delivery count and repeated processing — see at3-03,
    where the pod's WHOLE log names this id exactly twice, with zero redelivery lines and zero
    "Completed message" lines.
