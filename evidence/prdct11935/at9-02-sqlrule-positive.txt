# PRDCT-11935 at9-02 — the scanner SQL rule POSITIVE, on the live lab replica
# FILE LAST WRITTEN 2026-08-23T19:46:06Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · captured 2026-08-23T19:04:50Z
#
# SCOPE OF THIS RE-RUN, STATED UP FRONT. This round is READ-ONLY against Azure: nothing is created,
# deleted or modified. The previous at9-02 drove FRESH blobs through the chain to raise new Event Grid
# deliveries; writing a blob is a create, so that half is NOT re-driven here and no fabricated
# substitute is offered. What IS re-measured, live and now:
#   (a) the rule text and the subscription that carries it, off the live namespace;
#   (b) the delivered messages ALREADY resident on the scanner lane, peeked (peek is a non-destructive
#       AMQP management read — it does not lock, complete, abandon or delete anything), which show the
#       rule's matches as Service Bus itself recorded them, with the Event Grid delivery stamp intact.
#
# lab: subscription b3f77526-827b-4354-9166-bcbff86b9749 · rg prdct11935-rg · namespace prdct11935bus
#      topic byoc-events · queue endpoint-asset-ingestion-prdct11935

=========================================================================================================
## 1. THE RULE, LIVE
=========================================================================================================
$ az servicebus topic subscription rule list -g prdct11935-rg --namespace-name prdct11935bus \
    --topic-name byoc-events --subscription-name only-mcp-scanner \
    --query "[].{subscription:'only-mcp-scanner', rule:name, sql:sqlFilter.sqlExpression, filterType:filterType}" -o json
[
  {
    "filterType": "SqlFilter",
    "rule": "mcp-scanner-rule",
    "sql": "sys.Label LIKE '%source=mcp-scanner/%'",
    "subscription": "only-mcp-scanner"
  }
]

  EXPECTED FILTER TEXT: sys.Label LIKE '%source=mcp-scanner/%'
  MEASURED FILTER TEXT: sys.Label LIKE '%source=mcp-scanner/%'
  -> MATCH, byte for byte, and identical to the authored terraform
     (infrastructure modules/onyx/cell-azure-instance/service_bus.tf:342-348):
        resource "azurerm_servicebus_subscription_rule" "byoc_endpoint_asset_ingestion_filter" {
          count           = var.create_service_bus && var.create_byoc_events_topic && var.create_endpoint_asset_ingestion_queue ? 1 : 0
          name            = "only-mcp-scanner"
          subscription_id = azurerm_servicebus_subscription.byoc_to_endpoint_asset_ingestion[0].id
          filter_type     = "SqlFilter"
          sql_filter      = "sys.Label LIKE '%source=mcp-scanner/%'"
        }

  NAMING DRIFT (still present — see at9-03 §D1 for the full drift ledger): the terraform names the
  SUBSCRIPTION `to-endpoint-asset-ingestion` and the RULE `only-mcp-scanner`; the lab has the
  subscription named `only-mcp-scanner` and the rule named `mcp-scanner-rule`. The FILTER TEXT and the
  forwarding target are identical, which is what this test turns on.

=========================================================================================================
## 2. THE SUBSCRIPTION AND WHERE IT FORWARDS
=========================================================================================================
$ az servicebus topic subscription list -g prdct11935-rg --namespace-name prdct11935bus --topic-name byoc-events \
    --query "[].{name:name, forwardTo:forwardTo, maxDeliveryCount:maxDeliveryCount, dlqOnExpiry:deadLetteringOnMessageExpiration, ttl:defaultMessageTimeToLive, lockDuration:lockDuration}" -o json
  (only-mcp-scanner row; the full 13-row output is in at9-03 §2)
  {
    "dlqOnExpiry": false,
    "forwardTo": "endpoint-asset-ingestion-prdct11935",
    "lockDuration": "PT1M",
    "maxDeliveryCount": 10,
    "name": "only-mcp-scanner",
    "ttl": "P10675199DT2H48M5.4775807S"
  }

=========================================================================================================
## 3. REAL EVENT-GRID-DELIVERED MESSAGES THAT MATCHED THE RULE — peeked off the live lane
=========================================================================================================
# The scanner queue's ACTIVE depth is 0 (the consumer has drained it), so the matched deliveries that
# survive on the broker are the ones that dead-lettered. Every one of them is still a message the rule
# routed here, and each carries Event Grid's own `aeg-subscription-name` stamp proving it came through
# the blob -> Event Grid -> byoc-events -> only-mcp-scanner chain rather than being injected.
#
# $ /tmp/at9venv/bin/python /tmp/at9_xtab.py     (azure-servicebus peek_messages, non-destructive)
# cross-tab captured 2026-08-23T19:01:40.189492+00:00

## endpoint-asset-ingestion-prdct11935: 0 message(s) peeked
   Labels carrying source=extension/      : 0
   Labels carrying source=sensor-events/  : 0
   Labels carrying source=mcp-scanner/    : 0

## endpoint-asset-ingestion-prdct11935/$DeadLetterQueue: 119 message(s) peeked
      23  source-segment=(no source= segment in Label)       aeg-subscription-name=(none — not Event-Grid-delivered; injected directly onto the queue by a harness)
       3  source-segment=source=mcp-scanner                  aeg-subscription-name=(none — not Event-Grid-delivered; injected directly onto the queue by a harness)
      93  source-segment=source=mcp-scanner                  aeg-subscription-name=TO-BYOC-EVENTS
   Labels carrying source=extension/      : 0
   Labels carrying source=sensor-events/  : 0
   Labels carrying source=mcp-scanner/    : 96

  READ THAT TABLE: 93 messages on this lane were delivered by Event Grid subscription TO-BYOC-EVENTS
  and every single one carries a Label whose path segment is `source=mcp-scanner`. Not one carries
  `source=extension` or `source=sensor-events`. That is the positive: the rule matched only what it
  was written to match, on real broker-recorded traffic.
  The 23 with no source= segment and the 3 mcp-scanner ones with no aeg stamp were injected straight
  onto the QUEUE by earlier verifier harnesses, bypassing the topic entirely — they never passed
  through the rule and are named here rather than quietly counted as matches.

## a matched message in full — the Label the rule fired on, with Event Grid's delivery stamp:
{
 "message_id": "d2eb7c7e-8595-4af1-8da8-3aa18affd8cf",
 "Label (sys.Label)": "/blobServices/default/containers/ingestion/blobs/tenant=11935b00-0000-4000-8000-000000000b02/source=mcp-scanner/date=2026-08-20/vr21n1787258058-POS.log",
 "body.subject": "/blobServices/default/containers/ingestion/blobs/tenant=11935b00-0000-4000-8000-000000000b02/source=mcp-scanner/date=2026-08-20/vr21n1787258058-POS.log",
 "body.eventType": "Microsoft.Storage.BlobCreated",
 "delivery_count": 0,
 "dead_letter_reason": "scanner object has 1 non-blank line(s), none parseable as a JSON record — unprocessable",
 "aeg_properties": {
  "aeg-subscription-name": "TO-BYOC-EVENTS",
  "aeg-delivery-count": "0",
  "aeg-data-version": "",
  "aeg-metadata-version": "1",
  "aeg-event-type": "Notification"
 }
}

  Label == body.subject, verbatim — which is the delivery property at9-04 measures, and is what makes
  `sys.Label LIKE '%source=mcp-scanner/%'` able to fire at all.

=========================================================================================================
## VERDICT
=========================================================================================================
  POSITIVE: PASS on the current head.
    · live rule text == authored terraform rule text (exact string match)
    · 93 Event-Grid-delivered messages on the scanner lane, 93/93 with a source=mcp-scanner Label
    · 0 non-scanner Labels on the lane, from any delivery path
  NOT RE-DRIVEN THIS ROUND (stated, not substituted): a fresh blob write to raise a new BlobCreated
  event. That is a create, and this round is read-only.
