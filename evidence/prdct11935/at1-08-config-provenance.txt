# PRDCT-11935 at1-08 — config provenance: the live vector azure sink and the live consumer config,
# FILE LAST WRITTEN 2026-08-23T20:22:54Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
#                       both re-derived from the authored sources and diffed
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · image localhost:5001/onyx/endpoint-asset-ingestion:r46b-head · captured 2026-08-23T20:22:00Z
#
# RE-MEASURED THIS ROUND on the current shipped head, against flux-fleet @ a4493c1e5ac0 (the same
# checkout at8 renders). Both halves are re-run mechanically here — the scripts are next to this file
# (at1-08-derive-sink.py, at1-08-rediff.py, at1-08-cfg-diff.py) and the normalized inputs/outputs are
# at1-08-rendered-sink.yaml, at1-08-live-sink-readback.yaml, at1-08-rediff-output.txt.
#
# The claim being proven: the vector sink and the consumer config that carried the at1-02..05 scan
# are the AUTHORED cell config with per-cell substitutions, not something hand-typed for the test.

=================== PART 1 — the vector azure_blob sink IS the authored render ===================

## Authored source (the file at8 validates):
##   ~/flux-fleet/clusters/ox-az-p-eus2-c02/instances/saas/vector-sas-generator.yaml
##   -> data."generate-vector-config.sh" -> the `cat > /etc/vector-dynamic/vector.yaml << VECTOREOF`
##      heredoc -> sinks.azure_blob_sink
$ git -C ~/flux-fleet rev-parse HEAD
    a4493c1e5ac0545fdf7fcd2f349060288111c42c
$ git -C ~/flux-fleet log -1 --format="%H %ad %s" -- clusters/ox-az-p-eus2-c02/instances/saas/vector-sas-generator.yaml
    80b341a25f05da1d075a2153861189c1a2edf424 Wed Aug 5 17:50:05 2026 +0300 PRDCT-4368 | azure vector: disable the azure_blob health check, not the SAS (#2110)

## THE RE-DIFF, run this round: read the live sink back out of the cluster ConfigMap, extract the
## authored stanza out of the flux-fleet heredoc, normalize away ONLY the three per-cell
## substitutions, and diff. Exit 0 / empty diff = the live sink IS the authored render.
$ python3 /tmp/prdct11935/r32/rediff.py   # copy: at1-08-rediff.py
  ## the 3 sanctioned substitutions (authored token -> live lab value):
     connection_string: '${CONNECTION_STRING}'  ->  'BlobEndpoint=https://prdct11935st.blob.core.windows.net;SharedAccessSignature=<user-delegation SAS, racwl, +24h — minted by the authored generator procedure>'
     container_name: '${CONTAINER_NAME}'  ->  'ingestion'
     inputs: ['s3_depth_guard']  ->  ['extract_tenant_id']
  
  ## normalized diff (empty = the live sink IS the authored render):
     (empty — byte-identical after the 3 substitutions)
  $ echo $?
  0

## the two normalized documents the diff compared, printed in full so the emptiness is checkable:
##   authored (at1-08-rendered-sink.yaml):
    acknowledgements:
      enabled: true
    batch:
      max_bytes: 10485760
      max_events: 10000
      timeout_secs: 20
    blob_append_uuid: true
    blob_prefix: tenant={{ x-consumer-username }}/source={{ x-source-type }}/date=%F/{{
      x-principal-suffix }}
    buffer:
      max_size: 21395124224
      type: disk
      when_full: block
    compression: none
    connection_string: <substituted:connection_string>
    container_name: <substituted:container_name>
    encoding:
      codec: json
    framing:
      method: newline_delimited
    healthcheck:
      enabled: false
    inputs: <substituted:inputs>
    type: azure_blob
##   live, read back from cm/onyx-onyxd-vector (at1-08-live-sink-readback.yaml):
    acknowledgements:
      enabled: true
    batch:
      max_bytes: 10485760
      max_events: 10000
      timeout_secs: 20
    blob_append_uuid: true
    blob_prefix: tenant={{ x-consumer-username }}/source={{ x-source-type }}/date=%F/{{
      x-principal-suffix }}
    buffer:
      max_size: 21395124224
      type: disk
      when_full: block
    compression: none
    connection_string: <substituted:connection_string>
    container_name: <substituted:container_name>
    encoding:
      codec: json
    framing:
      method: newline_delimited
    healthcheck:
      enabled: false
    inputs: <substituted:inputs>
    type: azure_blob

## and the live sink verbatim out of the cluster, with only the SAS redacted:
$ kubectl -n onyxd get cm onyx-onyxd-vector -o jsonpath='{.data.vector\.yaml}' | yq '.sinks.azure_blob_ingestion'
    acknowledgements:
      enabled: true
    batch:
      max_bytes: 10485760
      max_events: 10000
      timeout_secs: 20
    blob_append_uuid: true
    blob_prefix: tenant={{ x-consumer-username }}/source={{ x-source-type }}/date=%F/{{
      x-principal-suffix }}
    buffer:
      max_size: 21395124224
      type: disk
      when_full: block
    compression: none
    connection_string: BlobEndpoint=https://prdct11935st.blob.core.windows.net;SharedAccessSignature=<redacted>
    container_name: ingestion
    encoding:
      codec: json
    framing:
      method: newline_delimited
    healthcheck:
      enabled: false
    inputs:
    - extract_tenant_id
    type: azure_blob
    
## The three sanctioned substitutions, and nothing else:
##   connection_string  ${CONNECTION_STRING} -> BlobEndpoint=https://prdct11935st.blob.core.windows.net;
##                      SharedAccessSignature=<redacted>. The VALUE is minted by running the AUTHORED
##                      generator procedure verbatim against the lab account (user-delegation SAS,
##                      permissions racwl, +24h, --auth-mode login --as-user). Never written to an artifact.
##   container_name     ${CONTAINER_NAME}    -> ingestion
##   inputs             [s3_depth_guard]     -> [extract_tenant_id]   (the ONE Tilt-forced remap:
##                      s3_depth_guard is defined in the same authored heredoc, not in the Tilt cm's
##                      transform graph, so the sink attaches to s3_depth_guard's own input instead.)
##
## Observable consequences of it being the AUTHORED stanza rather than a hand-typed one, all visible
## in this round's own run: batch.timeout_secs=20 -> the 6 scanner payloads of the at1-02 scan
## coalesced into exactly TWO blobs 56s apart (20:06:15Z and 20:07:11Z), and healthcheck.enabled=false
## + acknowledgements.enabled=true + the 20GiB disk buffer are all present.

## the sink is alive and delivering. NOT CAPTURABLE THIS ROUND, stated rather than faked: the
## component's BOOT lines ("Building new sink" / "Using SAS token authentication") were emitted at
## 2026-08-23T19:06:12Z when the pod started, and the container's retained log buffer now begins at
## 19:48:24Z — those two lines have rotated out and re-creating them would mean restarting vector
## mid-evidence. What IS capturable is the same sink doing its job during this round's own scans:
$ kubectl -n onyxd logs onyx-onyxd-vector-0 | grep -c '"component_id":"azure_blob_ingestion"'
    184
$ kubectl -n onyxd logs onyx-onyxd-vector-0 | grep '"component_id":"azure_blob_ingestion"' | tail -2
    {"timestamp":"2026-08-23T20:17:54.540118Z","level":"DEBUG","message":"Internal log [reuse idle connection for (\"https\", prdct11935st.blob.core.windows.net)] is being suppressed to avoid flooding.","target":"hyper_util::client::legacy::pool","span":{"name":"request"},"spans":[{"component_id":"azure_blob_ingestion","component_ki
    {"timestamp":"2026-08-23T20:17:54.642883Z","level":"DEBUG","message":"Internal log [pooling idle connection for (\"https\", prdct11935st.blob.core.windows.net)] is being suppressed to avoid flooding.","target":"hyper_util::client::legacy::pool","span":{"name":"request"},"spans":[{"component_id":"azure_blob_ingestion","component_
    performing request PUT 'https://prdct11935st.blob.core.windows.net/ingestion/tenant=61750f27-8fbd-42a4-80cf-4191cfb2371a%2Fsource=mcp-scanner%2Fdate=2026-08-23%2F1787516274-a919ed9d-71cb-4ecd-b12c-99d830347e03.log?<SAS query redacted>
    (the PUT is the LAST line above: the sink writing this run's own object
     1787516274-a919ed9d-…log into prdct11935st/ingestion. The SAS query string is redacted here —
     vector logs it in full and it never goes into an artifact.)
$ kubectl -n onyxd logs onyx-onyxd-vector-0 --since=20m | grep -E '"level":"(ERROR|WARN)"' | grep -v '"intentional":true' | wc -l
    0
$ kubectl -n onyxd get pod onyx-onyxd-vector-0 -o custom-columns=NAME:.metadata.name,READY:.status.containerStatuses[0].ready,START:.status.startTime
    NAME                  READY   START
    onyx-onyxd-vector-0   true    2026-08-23T19:06:12Z

=================== PART 2 — the consumer config vs the authored Azure-cell overlay ===================

## Authored source: the same render at8 validates —
##   kustomize build --load-restrictor LoadRestrictionsNone \
##     clusters/ox-az-p-eus2-c02/instances/saas/onyx-app
##   | yq 'select(.kind=="HelmRelease" and .metadata.name=="onyx-app")
##          | .spec.values."endpoint-asset-ingestion".configMap.data'
$ bash /tmp/prdct11935/at8-render.sh
    ### kustomize build clusters/ox-az-p-eus2-c02/instances/saas/onyx-app
    ### helm template onyx-app (chart source infra/helm/onyx-app) -n saas
    ok: saas

## the authored Azure-cell overlay it produced:
    alert_processing_asb:
      fully_qualified_namespace: ${servicebus_namespace}
      queue_name: ${alert_processing_queue_name}
    asset_ingestion_asb:
      fully_qualified_namespace: ${endpoint_asset_ingestion_servicebus_namespace}
      queue_name: ${endpoint_asset_ingestion_queue_name}
    asset_ingestion_s3:
      aws_region: ""
      endpoint: ""
    blob:
      account_url: ${blob_account_url}
      container_name: ${blob_container_name}
    cloud: azure
    concurrency: 5
    database:
      host: ${rds_endpoint}
      name: postgres
      port: 5432
      ssl_mode_disabled: false
    error_backoff_seconds: 5
    logging:
      level: INFO
      render_json: true
    max_messages: 10
    releases_kvs:
      kvs_arn: ${releases_kvs_arn:=}
      writer_role_arn: ${releases_kvs_writer_role_arn:=}
    telemetry:
      otlp_endpoint: groundcover-sensor.groundcover.svc.cluster.local:4317
      service_name: endpoint-asset-ingestion
    temporal_server:
      host: temporal-frontend.mgmt.svc.cluster.local
      namespace: ${temporal_namespace}
      port: 7233

## the LIVE consumer config (cm/eai-azure), verbatim:
$ kubectl -n onyxd get cm eai-azure -o jsonpath={.data.config\.yaml}
    concurrency: 2
    error_backoff_seconds: 5
    lane: scanner
    cloud: azure
    logging:
      level: INFO
      render_json: false
    max_messages: 10
    database:
      host: postgres-rw
      name: postgres
      port: 5432
      ssl_mode_disabled: true
    telemetry:
      otlp_endpoint: null
      service_name: endpoint-asset-ingestion
    temporal_server:
      host: onyx-temporal-frontend
      namespace: default
      port: 7233
    asset_ingestion_s3:
      aws_region: us-east-1
      endpoint: http://localstack:4566
    asset_ingestion_sqs:
      aws_region: us-east-1
      endpoint: http://localstack:4566
      queue_url: ""
      visibility_timeout: 600
      wait_time_seconds: 20
    asset_ingestion_asb:
      fully_qualified_namespace: "prdct11935bus.servicebus.windows.net"
      queue_name: "endpoint-asset-ingestion-prdct11935"
    blob:
      account_url: https://prdct11935st.blob.core.windows.net
      container_name: ingestion
    alert_processing_asb:
      fully_qualified_namespace: "prdct11935bus.servicebus.windows.net"
      queue_name: "alert-processing-prdct11935"

## MECHANICAL key-by-key comparison (at1-08-cfg-diff.py walks EVERY key on either side and
## classifies it — no key is hand-picked, nothing is omitted):
$ python3 at1-08-cfg-diff.py
  key                                             authored (flux-fleet saas cell)                 live (cm/eai-azure)                           classification
  --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  alert_processing_asb.fully_qualified_namespace  ${servicebus_namespace}                         prdct11935bus.servicebus.windows.net          PLACEHOLDER -> lab value substituted  [DECISIVE]
  alert_processing_asb.queue_name                 ${alert_processing_queue_name}                  alert-processing-prdct11935                   PLACEHOLDER -> lab value substituted  [DECISIVE]
  asset_ingestion_asb.fully_qualified_namespace   ${endpoint_asset_ingestion_servicebus_namespac  prdct11935bus.servicebus.windows.net          PLACEHOLDER -> lab value substituted  [DECISIVE]
  asset_ingestion_asb.queue_name                  ${endpoint_asset_ingestion_queue_name}          endpoint-asset-ingestion-prdct11935           PLACEHOLDER -> lab value substituted  [DECISIVE]
  asset_ingestion_s3.aws_region                                                                   us-east-1                                     DIFFERS  [DECISIVE]
  asset_ingestion_s3.endpoint                                                                     http://localstack:4566                        DIFFERS  [DECISIVE]
  asset_ingestion_sqs.aws_region                  <ABSENT>                                        us-east-1                                     LAB-ONLY (not in the authored cell)
  asset_ingestion_sqs.endpoint                    <ABSENT>                                        http://localstack:4566                        LAB-ONLY (not in the authored cell)
  asset_ingestion_sqs.queue_url                   <ABSENT>                                                                                      LAB-ONLY (not in the authored cell)
  asset_ingestion_sqs.visibility_timeout          <ABSENT>                                        600                                           LAB-ONLY (not in the authored cell)
  asset_ingestion_sqs.wait_time_seconds           <ABSENT>                                        20                                            LAB-ONLY (not in the authored cell)
  blob.account_url                                ${blob_account_url}                             https://prdct11935st.blob.core.windows.net    PLACEHOLDER -> lab value substituted  [DECISIVE]
  blob.container_name                             ${blob_container_name}                          ingestion                                     PLACEHOLDER -> lab value substituted  [DECISIVE]
  cloud                                           azure                                           azure                                         IDENTICAL  [DECISIVE]
  concurrency                                     5                                               2                                             DIFFERS
  database.host                                   ${rds_endpoint}                                 postgres-rw                                   PLACEHOLDER -> lab value substituted
  database.name                                   postgres                                        postgres                                      IDENTICAL
  database.port                                   5432                                            5432                                          IDENTICAL
  database.ssl_mode_disabled                      False                                           True                                          DIFFERS
  error_backoff_seconds                           5                                               5                                             IDENTICAL
  lane                                            <ABSENT>                                        scanner                                       LAB-ONLY (not in the authored cell)
  logging.level                                   INFO                                            INFO                                          IDENTICAL
  logging.render_json                             True                                            False                                         DIFFERS
  max_messages                                    10                                              10                                            IDENTICAL  [DECISIVE]
  releases_kvs.kvs_arn                            ${releases_kvs_arn:=}                           <ABSENT>                                      AUTHORED-ONLY (not in the lab cm)
  releases_kvs.writer_role_arn                    ${releases_kvs_writer_role_arn:=}               <ABSENT>                                      AUTHORED-ONLY (not in the lab cm)
  telemetry.otlp_endpoint                         groundcover-sensor.groundcover.svc.cluster.loc  None                                          DIFFERS
  telemetry.service_name                          endpoint-asset-ingestion                        endpoint-asset-ingestion                      IDENTICAL
  temporal_server.host                            temporal-frontend.mgmt.svc.cluster.local        onyx-temporal-frontend                        DIFFERS
  temporal_server.namespace                       ${temporal_namespace}                           default                                       PLACEHOLDER -> lab value substituted
  temporal_server.port                            7233                                            7233                                          IDENTICAL
  
  ### the DECISIVE keys only (the ones that choose transport + storage):
    alert_processing_asb.fully_qualified_namespace  authored='${servicebus_namespace}'  live='prdct11935bus.servicebus.windows.net'   -> PLACEHOLDER -> lab value substituted
    alert_processing_asb.queue_name                 authored='${alert_processing_queue_name}'  live='alert-processing-prdct11935'   -> PLACEHOLDER -> lab value substituted
    asset_ingestion_asb.fully_qualified_namespace   authored='${endpoint_asset_ingestion_servicebus_namespace}'  live='prdct11935bus.servicebus.windows.net'   -> PLACEHOLDER -> lab value substituted
    asset_ingestion_asb.queue_name                  authored='${endpoint_asset_ingestion_queue_name}'  live='endpoint-asset-ingestion-prdct11935'   -> PLACEHOLDER -> lab value substituted
    asset_ingestion_s3.aws_region                   authored=''  live='us-east-1'   -> DIFFERS
    asset_ingestion_s3.endpoint                     authored=''  live='http://localstack:4566'   -> DIFFERS
    blob.account_url                                authored='${blob_account_url}'  live='https://prdct11935st.blob.core.windows.net'   -> PLACEHOLDER -> lab value substituted
    blob.container_name                             authored='${blob_container_name}'  live='ingestion'   -> PLACEHOLDER -> lab value substituted
    cloud                                           authored='azure'  live='azure'   -> IDENTICAL
    max_messages                                    authored=10  live=10   -> IDENTICAL

## READ THE CLASSIFICATION HONESTLY — what matches, and what does not:
##
##  · Every AZURE-DECISIVE key is either IDENTICAL to the authored value (cloud: azure, max_messages: 10)
##    or is an authored ${placeholder} filled with this cell's value (the two ASB namespaces + queue
##    names, the blob account_url + container_name). That is the whole transport+storage decision, and
##    it comes from the authored overlay.
##
##  · TWO decisive keys are marked DIFFERS and that is a REAL delta, not a rounding: the authored cell
##    blanks asset_ingestion_s3.{aws_region,endpoint} ("" / "") while the lab cm carries
##    us-east-1 / http://localstack:4566. FIVE more lab-only keys exist for the same reason
##    (asset_ingestion_sqs.{aws_region,endpoint,queue_url,visibility_timeout,wait_time_seconds}).
##    They are there because this ONE ConfigMap is the lab harness's shared shape. They are INERT, and
##    inert is MEASURED, not asserted, on two independent counts:
##      (a) the transport guard keys on the QUEUE URL, and the live asset_ingestion_sqs.queue_url is
##          the empty string — so the AWS branch is never selected. at4 proves the same guard DOES
##          fire the moment a real queue_url is present.
##      (b) at1-08's sibling capture at1-04 counts the pod's WHOLE log and finds ZERO occurrences of
##          SQS / boto3 / botocore / amazonaws / S3Client / localstack — i.e. no AWS client was ever
##          constructed at runtime, whatever these leaves say.
##
##  · The remaining DIFFERS are lab-vs-cloud plumbing with no bearing on the lane: concurrency 5->2,
##    database.host/ssl_mode_disabled (in-cluster CNPG vs RDS), logging.render_json (human-readable
##    logs in the lab), telemetry.otlp_endpoint (no groundcover collector here),
##    temporal_server.host/namespace. releases_kvs.* is AUTHORED-ONLY (an AWS KVS binding the lab has
##    no equivalent for) and `lane: scanner` is LAB-ONLY (the lab runs one lane per deployment).
##
## NOT CLAIMED: this file does NOT claim the live cm is byte-identical to the authored render — it
## demonstrably is not, and the diff above shows every difference. What it claims, and shows, is that
## every key that DECIDES azure transport and azure storage is the authored cell's own, and that the
## AWS-shaped leaves the lab cm carries are provably not exercised.
