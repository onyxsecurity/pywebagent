# PRDCT-11935 at7-48-lastscanned-writer.txt — amendment_r31 part (2b): the complete writer inventory for desktop_agent_applications.last_scanned_at at head, re-derived this window
# FILE LAST WRITTEN 2026-08-24T01:38:07Z
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · image localhost:5001/onyx/endpoint-asset-ingestion:r46b-head (endpoint-asset-ingestion, azure-mode writer, read live) + localhost:5001/onyx/temporal-python-worker:r50-head (posture/issue-detection worker, read live) · captured 2026-08-24T01:38:07Z

Re-derived on head 2b60fdc4df25188104e737e6311087681355eff8 (clean tree) this window. Line numbers
below are HEAD line numbers, read out of the checkout now — not carried from a prior round.

==============================================================================
THE COMPLETE WRITER INVENTORY (every code path that can advance the column)
==============================================================================
w1  common/repository/desktop_agent_application.py — the scanner upsert (BOTH lanes use it)
      :209:            scanned_at = last_scanned_at if last_scanned_at is not None else datetime.now(UTC)
      :215:                "last_scanned_at": scanned_at,
      :225:                "last_scanned_at": scanned_at,
    reached from python_temporal_worker/integrations/desktop_agent_creator/activities.py:
      :1878:                    await desktop_agent_repo.upsert_desktop_agent_application(

w2  common/repository/desktop_agent_application.py — the guard-status aggregate
      :311:    async def authoritatively_set_guard_statuses(
      :385:                    last_scanned_at=func.greatest(DesktopAgentApplication.last_scanned_at, clamped_at),

w3  READ-ONLY consumers (they do not write): issue_detection/activities.py
      :857:    last_scanned_at: datetime | None,
      :865:    scanned_at = _as_aware_utc(last_scanned_at)
      :912:            last_scanned_at=cast(datetime | None, application.last_scanned_at),

  DELTA vs the round-21 inventory this file used to carry: the openai_compliance posture
  processor no longer references the column at head —
      grep -rn 'last_scanned_at' python_temporal_worker/integrations/openai_compliance/ | wc -l -> 0
  so the head inventory is w1 (scanner upsert, both lanes) + w2 (guard aggregate). Nothing else
  in backend_python writes it; the Go side only reads/serialises it (ent generated code).

  Both writers are MONOTONIC: w1 stamps the payload's scanned_at (or now()); w2 uses
  greatest(existing, clamped_at). Neither can regress the value, and — critically — neither runs at
  all for a runtime that is not in the payload. THAT is the mechanism behind the staircase in at7-30:
  the subject is not advanced because no payload lists it, not because anything freezes it.

==============================================================================
WHY THE ATTRIBUTION IS A MECHANISM DEMONSTRATION, NOT A LOG JOIN
==============================================================================
amendment_r31 part (2b) asks for a direct log/workflow-id join of an observed CROSS-WINDOW advance to
the ingest that wrote it WHENEVER logs or histories covering one still exist, and otherwise for the
labelled MECHANISM demonstration. Checked this window:

  - the cross-window advances of asset 10729 that would need joining happened on 2026-08-19..08-23;
  - `kubectl logs` on both lane pods reaches only as far back as the current container start
    (eai-azure-7c4f9bd78c-hd7xd started 2026-08-23T22:08:57Z; the AWS-lane pod restarted 2026-08-23T21:5xZ),
    and neither retains 08-19..08-21;
  - Temporal history for the scanner-object bridges of those windows has aged out of the visible list.
  So no log covering a historical cross-window advance still exists, and the MECHANISM route applies.

==============================================================================
THE MECHANISM DEMONSTRATION (this window, in-window, and DELIBERATELY EXCLUDED from the staircase)
==============================================================================
The demonstration is the FIRST HALF of this very window, and it is a real front-door observation of
w1 advancing exactly this row:

  00:29:44Z  scan 1 posted through Kong (key-auth 200) WITH openclaw installed
             -> the payload's agent list contained openclaw
  00:30:02Z  the azure chain settled the object on eai-azure (outcome=processed,
             s3_key tenant=61750f27-.../source=mcp-scanner/date=2026-08-24/1787531397-a886be06-....log)
  00:31:26.226423+00   desktop_agent_applications.last_scanned_at for asset 10729 ADVANCED to ingest
             wall-clock — written by w1, through the full front door (Kong -> Blob -> Event Grid ->
             Service Bus -> azure-mode consumer). No in-process writer call, no hand-seeded row, no
             hand-uploaded blob.

  01:22:39Z  the runtime was really uninstalled (npm uninstall -g openclaw; absence proven 4 ways)
  01:22:47Z  scan 2 posted through the same front door WITHOUT openclaw installed
             -> Detector scanned {"detector":"openclaw","outcome":"not_installed","instances":[]}
             -> agents_detected 4, openclaw absent from the agent list
  01:24:49Z  the same row's last_scanned_at is STILL 00:32:03.449330+00 while four controls advanced

  The pair is the demonstration: the identical code path, the identical transport, the identical row —
  advances when the payload lists the runtime, does not advance when it does not. Per-unique_identifier
  count stayed 1, class totals and is_deleted unchanged throughout (at7-41).

  WINDOW BOOKKEEPING, as the amendment requires:
    demonstration window : 00:29:44Z - 00:32:03Z (scan 1) — EXCLUDED from the staircase
    staircase window     : 01:22:18Z - 01:24:49Z (the paired scan 2) — the at7-30 measurement
  This capture is labelled MECHANISM attribution. It is NOT claimed to be a log join of the historical
  cross-window advances, and no such claim is made anywhere in this bundle.
