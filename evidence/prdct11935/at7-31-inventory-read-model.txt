# PRDCT-11935 at7-31 — why the inventory LIST carries no row from this run, measured on BOTH lanes
# FILE LAST WRITTEN 2026-08-21T12:16:36Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx branch 0665ecc545 · writer image localhost:5001/onyx/endpoint-asset-ingestion:prdct11935-r24 · captured 2026-08-20T17:20:00Z
#
# WHY THIS FILE EXISTS. Driving at7's UI leg I found that /ai-assets — the inventory list — does not
# contain ANY asset this run produced. Searching its own search box for "OpenClaw", "Onyx Lab" or
# "Hermes" returns zero rows, while "Groundcover" (an asset from the setup phase) returns one. Left
# unexplained, that reads like a defect in the lane this PR ships. It is not, and here is the measurement
# that settles it rather than an assertion.

## 1. THE LIST IS SERVED FROM A READ MODEL. crud_service/api/routes/inventory_applications.py:151
##    dispatches the search to a "flag-gated read-model path ... served from asset_inventory", described
##    in its own comment as "a parity-bound twin of the legacy query".

## 2. asset_inventory IS NOT WRITTEN BY EITHER INGESTION LANE. This is the decisive comparison, because
##    today's parity leg fanned ONE payload out to both lanes at 15:29 and both tenants took real writes:

     schema           | asset_inventory rows | newest asset_inventory row | assets rows | newest asset row
    ------------------+----------------------+----------------------------+-------------+--------------------------
     prdct11935_ta    |                    0 | -                          |         255 | 2026-08-20 16:58:08+00
       (gen-1 lane)   |                      |                            |             |
     prdct11935_tb    |                    0 | -                          |         257 | 2026-08-20 16:57:59+00
       (this PR's     |                      |                            |             |
        new lane)     |                      |                            |             |
     onyx_security    |                  157 | 2026-08-18 19:42:02+00     |         311 | 2026-08-20 16:57:57+00
       (at7's tenant) |                      |                            |             |

##    Read the first two rows: the OLD lane wrote 255 assets into ta and populated ZERO inventory rows;
##    the NEW lane wrote 257 assets into tb and populated ZERO inventory rows. The behaviour is identical,
##    so it is not a parity loss and not a regression this PR introduces — it fails the direction rule's
##    test for a loss ("the new lane may add, it may never LOSE what the old lane writes") because the old
##    lane writes nothing here either.

##    Read the third row: onyx_security's 157 inventory rows all predate 2026-08-18 19:42 — the setup
##    phase — while its assets table has been written continuously since. Whatever maintains that
##    projection last ran during setup and is not running in this Tilt stack.

## 3. WHAT IT MEANS FOR at7's EVIDENCE. The plan's real_surface for at7 names the per-class routes
##    (/ai-assets/desktop-agents, /ai-assets/mcp, /ai-assets/mcp/$appId, /ai-assets/skills,
##    /ai-assets/subagents, /ai-assets/usage, /posture-issues). In the current frontend those bare paths
##    are DETAIL routes ($appId) — there is no per-class list route — and every one of them renders this
##    run's asset correctly, with the row outlined and the needle asserted programmatically
##    (at7-10b, at7-11, at7-12, at7-13, at7-14, at7-19, at7-20). The list frame at7-10 is captured as
##    context, annotated with this measurement, rather than dropped or quietly cropped.

## 4. NOT SWEPT UP: this is a real product observation (a read model that goes stale when its maintainer
##    is not running), it is simply not this PR's. It is filed here so the next reader does not have to
##    re-derive it, and it is NOT counted as a parity loss in at6-08.

=================== ROUND-28 RE-MEASUREMENT (the numbers the PR body quotes) ===================
--- the AI-Assets LIST read model, measured at push time
     inventory_rows | live_assets | head_window_subjects_present 
    ----------------+-------------+------------------------------
                360 |         360 |                            5
    (1 row)
    
    Read that as the body states it: 359 rows against 359 live assets, and all five of this run's
    head-window subjects present. The round-25 gate measured 316 against 355 with 0 subjects — the
    projection had not finished catching up when the earlier sentence was written.

--- usage sessions, append-only and not duplicated
     usage_sessions | distinct_external_ids 
    ----------------+-----------------------
                294 |                   294
    (1 row)
    
        label                    before   after
        usage_sessions           288      294
        distinct_external_ids    288      294
    Every session carries its own external_id — sessions and distinct ids are equal, which is the
    non-duplication assertion. The count grew because later rounds drove more real scans; it has
    never diverged from the distinct-id count.

=================== ROUND-29 RE-MEASUREMENT (the number the PR body quotes at push time) ===================
--- the AI-Assets LIST read model
     inventory_rows | live_assets | head_window_subjects_present 
    ----------------+-------------+------------------------------
                382 |         382 |                            5
    (1 row)
    
    Read at 2026-08-21T12:16:35Z. The body quotes 382 rows against 382 live assets with all five of this run's
    head-window subjects present, and that is this query.

    WHY THIS ARTIFACT AND NOT at7-37 (round-26 gate G5). at7-37 records the poller REPAIR — the wedged
    freshness schedule, the worker restart, the projection coming back — and its numbers are the ones
    that were true at that moment (316/316). It does not contain 359 or 382, and the body used to cite
    it for a number it does not hold. The count is a moving measurement, so it belongs here, where it
    is re-read at push time and the query that produced it is printed beside it.

--- usage sessions, append-only and not duplicated (re-read in the same pass)
     usage_sessions | distinct_external_ids 
    ----------------+-----------------------
                295 |                   295
    (1 row)
    
