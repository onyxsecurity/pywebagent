# PRDCT-11935 at5-01 POST-FIX — the gen-2 scanner-event-consumer REFUSES ONYX__CLOUD=azure instead of acking and dropping
# FILE LAST WRITTEN 2026-08-21T09:28:02Z
# onyx branch a2486bb33f (full a2486bb33f835af24f5850e4f4c5eb2e8b02afb9) · image localhost:5001/onyx/scanner-event-consumer:prdct11935-r26 · pod scanner-event-consumer-azure-prefix-5c48d8f544-lvc7q
# captured 2026-08-21T09:28:02Z
#
# THE IMAGE, PROVEN — not asserted. This artifact's capture ran on an image built from THIS head with the
# repo's own Dockerfile.dev, and the proof is mechanical: every .py under /app/src sha256'd inside the
# image against the head worktree — 4741 head files vs 4742 in the image, **one** differing line, and it
# is an EMPTY /app/src/__init__.py the build creates (sha e3b0c44298fc… is the hash of zero bytes). No
# source file differs. Digests: endpoint-asset-ingestion sha256:17dbec7e…, scanner-event-consumer
# sha256:4c7af190…, mcp-gateway-event-consumer sha256:2464ec5a…. See evidence/at6-12-image-provenance.txt.
#
# WHY THIS FILE WAS RE-DRIVEN THIS ROUND. The round-25 gate found this artifact self-certifying as
# head-exact while stamping `0665ecc545 / :prdct11935-r25` — an image built two lane-touching commits
# back — and at10-04, generated from this very header, listed it as "1 lane-touching commit(s) landed
# after it: a2486bb33f". Rather than argue that the head commit cannot reach a service that exits at
# config validation, the capture was simply re-run on the head-exact image. Everything below is that run.
#
# THE SHAPE UNDER TEST: the gen-2 AWS-only consumer pointed at the Azure lane. Pre-fix it booted green
# and ACKED every real Event Grid delivery without dispatching one — the silent-drop trap this AT exists
# to close. The image here was rebuilt from THIS head for this capture (see the provenance block above).

## 1. WHAT KUBERNETES SHOWS, and the honest nuance
    -- kubectl:
    scanner-event-consumer-azure-prefix-5c48d8f544-lvc7q   1/1   Running   0     3m32s
    -- readinessProbe:
    
    -- conditions:
    PodReadyToStartContainers=True 
    Initialized=True 
    Ready=True 
    ContainersReady=True 
    PodScheduled=True
    HONEST NUANCE, because the pod's READY column would otherwise mislead: this throwaway probe
    deployment carries NO readinessProbe (the field is empty above) and the dev image's CMD is
    `uvicorn --reload`, whose reloader parent keeps PID 1 alive after the app's lifespan raises. So
    Kubernetes shows READY=1/1 for a process that is serving nothing. That is a property of MY probe
    manifest, not of the service. The measurements that matter are below.

## 2. THE REFUSAL, verbatim from the pod's own log
    -- the refusal, verbatim:
    pydantic_core._pydantic_core.ValidationError: 1 validation error for ScannerEventConsumerConfig
      Value error, scanner_event_consumer_service: ONYX__CLOUD=azure is not supported by this service (it implements aws only). Refusing to start rather than consuming a queue it cannot process. The Azure scanner-object lane is owned by endpoint_asset_ingestion (ONYX__CLOUD=azure), which parses Event Grid BlobCreated events off Service Bus and writes the assets in-process. [type=value_error, input_value={'cloud': 'azure', 'blob'...04b-a928-644f5a21ed12'}}, input_type=dict]
    ERROR:    Application startup failed. Exiting.

## 3. THE APP DID NOT START, AND CONSUMED NOTHING — measured
    -- did it consume ANYTHING? (must be 0 each)
         Completed message                0
         receive_messages                 0
         asset_ingestion_record_handled   0
         Dead-lettered message            0
         dispatch                         0

## 4. AND THE QUEUE WAS NOT DRAINED — the assertion that separates "refused" from "silently acked"
    queue depths (active / dead-lettered), read from Azure either side of the whole leg:
      BEFORE   scanner-events-prdct11935	47	0
mcp-gateway-events-prdct11935	47	0
      AFTER    scanner-events-prdct11935	47	0
mcp-gateway-events-prdct11935	47	0
    scanner-events-prdct11935 is unchanged. A pre-fix consumer in this exact shape drained its queue to zero
    while dispatching nothing; this one leaves every message where it found it.
