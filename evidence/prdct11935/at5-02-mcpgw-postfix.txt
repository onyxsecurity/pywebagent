# PRDCT-11935 at5-02 (POST-FIX) — mcp_gateway_event_consumer_service REFUSES cloud=azure
# FILE LAST WRITTEN 2026-08-23T19:39:22Z
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · image localhost:5001/onyx/mcp-gateway-event-consumer:r46b-head · captured 2026-08-23T19:39:22Z
#
# The pre-fix half on the frozen base shows this consumer ACCEPTING real Event Grid deliveries and
# ACKING them with nothing dispatched — the silent consume-and-drop this ticket's footgun names.
# RE-DRIVEN on the shipped head because the previous post-fix capture predates two owner rulings.

## deployment mcp-gateway-event-consumer-azure-prefix   pod mcp-gateway-event-consumer-azure-prefix-78f77dd69b-9zfj2   state: 1/1 Running 0
## exitCode= reason= restarts=0
## image actually running: localhost:5001/onyx/mcp-gateway-event-consumer:prdct11935-r26
## its declared cloud:
     cloud: azure
## the refusal, verbatim:
       Value error, mcp_gateway_event_consumer_service: ONYX__CLOUD=azure is not supported by this service (it implements aws only). Refusing to start rather than consuming a queue
      it cannot process. There is no Azure implementation of the MCP-GATEWAY object lane yet: the Azure side ships one Service Bus queue, endpoint-asset-ingestion, fed by a sys.Lab
     el 'source=mcp-scanner' rule, so gateway objects are not routed on an Azure cell at all. endpoint_asset_ingestion CAN run its gateway lane (ONYX__CLOUD=azure), but it would ne
     ed its own Service Bus queue and an Event Grid subscription rule for source=mcp-gateway, neither of which exists. Run this service on an AWS cell, or track the Azure gateway l
     ane before enabling one. [type=value_error, input_value={'cloud': 'azure', 'blob'...04b-a928-644f5a21ed12'}}, input_type=dict]
         For further information visit https://errors.pydantic.dev/2.12/v/value_error
