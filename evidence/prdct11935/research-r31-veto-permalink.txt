# PRDCT-11935 — round-31 owner-veto post (research, plan owner): amendments r31 (G5 lag re-scope + item-6 OVERTAKEN) + G6 no-change ruling
# captured 2026-08-21T17:39Z
# thread-leg message ts: 1787333760.193809 (posted 2026-08-21T17:36:00Z, issuer <@U0AH4F5L7A7> tagged)
# thread-leg permalink (THE round-31 veto permalink, minted via chat.getPermalink ok:true):
#   https://onyx-security.slack.com/archives/C0BCW4JDZB2/p1787333760193809?thread_ts=1787063683.640599&cid=C0BCW4JDZB2
# DM leg (May): ok:true, permalink https://onyx-security.slack.com/archives/D0B6V65HCEN/p1787333760466279
# emitter receipt: {"thread_ok": true, "issuer_tagged": true, "issuer_slack_id": "U0AH4F5L7A7", "dm_ok": true, "dm_user": "U09R2BCFDLG"}
# kind: plan-amendment-r31-veto
# plan re-frozen after this post: sha256 a2c558bc39a5efdaf2611546fa7b61ccaa5212acd971b6eb4c7e8ca300ffa833
# ---- POSTED TEXT VERBATIM (the emitter prepends its escalation header/tag line) ----
:scales: *ROUND-31 PLAN AMENDMENTS (owner-veto window) — PRDCT-11935, verify-r28 G5 + item 6 adjudicated by the plan owner; G6 adjudicated with NO plan change*

*Amendment 1 — `d-c2-lc-agent.amendment_r31` (G5, verify's route b):* the absolute term "last_scanned_at FROZEN" (amendment_r28 part 2) is WITHDRAWN — the run's own artifacts measured the uninstalled runtime's column advancing twice after uninstall (04:33:04 → 08:28:13 → 16:10:48) and withdrew the wording themselves (at7-30 §5, at7-44 §2). Code ground, re-checked from source this round: the column's complete writer set is (w1) the per-user ingest upsert — `common/repository/desktop_agent_application.py:209/:215/:225` via `desktop_agent_creator/activities.py:1878`, shared by BOTH lanes — which stamps ingest wall-clock for EVERY runtime listed in ANY ingested payload, *including re-posted historical payloads* (i.e. this run's own contractually-required re-drives); (w2) the guard aggregate (`:311/:385`); (w3) the compliance ensure-row (`openai_compliance/.../activities.py:102`). An absolute cross-window freeze is therefore unprovable-as-stated. The demand now requires the measurable property instead: the **single-scan staircase** (subject's value not advanced by the paired second scan while ≥2 same-machine controls advance — the existing at7-30 capture satisfies this verbatim) **plus the cross-window LAG with the writer NAMED AND EVIDENCED** (new pinned evidence `at7-48-lastscanned-writer.txt`: the writer inventory + the direct log join where logs survive, else a labeled MECHANISM-attribution demonstration re-POSTed through the full Kong→Blob→Event-Grid→Service-Bus chain; `at7-30` itself is now a pinned required_evidence path) **plus lag-honesty everywhere** (at7-18's "frozen at its scan-1 value" wording corrected; any absolute-freeze claim = FAIL). A stronger genuine-freeze proof on a subject absent from every re-posted payload stays admissible IN ADDITION (part 2d), so the engineer's parallel implement-first branch is not wasted. Parts 1/3/4 of amendment_r28 unchanged.

*Amendment 2 — `d-c3comment-saas` / `d-c3comment-centerpoint` `.amendment_r31` (item 6):* deliverable C's "fix the stale comment at release.yaml:64-66" is formally adjudicated OVERTAKEN-BY-UPSTREAM: flux-fleet `6964ee876` ("PRDCT-4368 | Remove all comments from the instance override files, and record the rules they carried (#2334)") deleted every comment from the instance override files (611 lines / 8 files, measured from its own diff) and relocated the rules to `docs/architecture/instance-override-rules.md` (+290), which this branch's endpoint-asset-ingestion section extends. The intent holds by deletion + relocation: the stale clause greps clean at the frozen base AND at head in BOTH instance files (per-instance evidence required in at8-04); re-introducing comments into either override file = FAIL. This formalizes the disposition at8-04 already ledgers — the one overtaken class that lacked a formal amendment.

*G6 — NO plan change (recorded in spec_interpretations (12) for the audit trail):* verify r28's offered alternative "promote at2-05 (the isolated-queue replay) into at2's required_evidence" is REFUSED — at2's pins stand exactly as written; the compliant vehicle is a re-driven at2-01/02/03 (same-MESSAGE lock-abandon redelivery on the at1-chain queue, FULL row contents + their diff, one tenant/window — feasibility proven first-hand by verify r28's own in-round 472-line redelivery diff). Promotion would import a quiesced/isolated-queue vehicle that at2's real_surface ("the same at1 chain … real Service Bus queue") does not name.

*Full text:* `test_plan.json` → `d-c2-lc-agent.amendment_r31`, `d-c3comment-*.amendment_r31`, `spec_interpretations` (12)-(13). Changed beyond the demands: at7 (agent-cell clauses + the new at7-48 evidence pin), at8 (flow step 4 + the at8-04 evidence item), at10 (carriage count → thirteen + C3-audit checks (u)-(y)). Nothing else touched; the plan-adversary gate ran three passes on the delta (pass 1: F1-F10, all resolved; pass 2: P2-1 record-precedes-measurement + P2-2 an unresolvable flux-fleet base ref — including the admission that research's own first-hand base check was a false-pass by construction, both resolved with error-checked re-measurement at fa943d66a8/68bdecfd1e70; pass 3: clean); sha256 re-frozen immediately after this post.

*Veto semantics (round-2/21/24/26/28/29/30 precedent):* silence at re-verify = proceed on no-objection. A reply here at ANY time routes via handle-feedback; if it restores the absolute-freeze reading or demands the comment be re-authored into the override files, the plan is re-emitted with a fresh sha256 and the run re-verifies against it.
