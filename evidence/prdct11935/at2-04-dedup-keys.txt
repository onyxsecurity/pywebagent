# PRDCT-11935 at2-04 — C11 dedup: every key SHOWN AT ITS ENFORCEMENT POINT, re-derived on head
# FILE LAST WRITTEN 2026-08-21T12:13:26Z
# onyx branch a2486bb33f (full a2486bb33f835af24f5850e4f4c5eb2e8b02afb9) · image localhost:5001/onyx/endpoint-asset-ingestion:prdct11935-r26 · pod eai-azure-66d8f8595d-8bh9j
# captured 2026-08-21T12:13:26Z · tenant onyx_security
#
# RE-DERIVED ROUND 29 (gate G9). d-c11-2 requires each key be shown WHERE IT IS ENFORCED, "never a bare
# assertion". The round-26 gate found the skills key failing that: the cite in circulation was
# "skills ON CONFLICT(asset_id) DO NOTHING (extract.py:275)", and at head that phrase is a PROSE COMMENT
# at extract.py:323 — there is no executable on_conflict for skills anywhere under
# endpoint_asset_ingestion/. The previous revision of this file went the other way and asserted the key
# with no enforcement site at all ("the SKILL.md sha256 IS the unique_identifier"), which is ALSO wrong:
# the identity is the skill DIRECTORY hash, not the file hash. Both are corrected below, and every line
# number here was grepped from the source at this head.

--- every identity this lane writes is content-derived or naturally keyed, so a replay lands on the same
    row rather than a second one. Enforcement points, in code at a2486bb33f835af24f5850e4f4c5eb2e8b02afb9:

      MCP asset id          calculate_mcp_id (mcp_asset_creation/activities.py:533) +
                            build_mcp_version_unique_identifier (:578)
                            -> {name}::{machine serial}[::{container}]::{version}

      desktop agent         endpoint_{app_name}::{machine serial}  (assets.unique_identifier)

      skill                 CORRECTED ROUND 29 — two things were wrong and both are fixed here.
                            IDENTITY: the asset's unique_identifier is the skill DIRECTORY hash, not the
                            SKILL.md file hash — _build_skill_unique_identifier(skill_dir_hash)
                            (skills_creation/activities.py:231-233, used at :302).
                            ENFORCEMENT: bulk_upsert_assets(..., insert_only=True)
                            (skills_creation/activities.py:504), which executes the real statement at
                            common/repository/asset.py:729:
                                insert(Asset).values(values)
                                  .on_conflict_do_nothing(index_elements=["unique_identifier"])
                            i.e. ON CONFLICT DO NOTHING on unique_identifier — write-once, never locking
                            or updating an existing row. That is the executable enforcement; the
                            extract.py text is a comment describing it, not the mechanism.

      subagent              subagent::{definition sha256} — content-addressed identity, writers.py:364

      MCP tool              (asset_id, name) upsert in the tenant tools table
                            (mcp_asset_creation/activities.py:1749 upsert_mcp_tools)

      asset_connections     UNIQUE (source_asset_id, destination_asset_id, connection_type)
                            (common_db/models/asset_connections.py:46)

      re-ingestion skip     mcp_ingestion_state keyed (unique_identifier, reporter_scope) with the
                            payload's content hash (common_db/models/mcp_ingestion_state.py:11)

      downstream workflows  deterministic workflow ids + WorkflowIDReusePolicy.REJECT_DUPLICATE
                            (mcp_downstreams.py:43), so a replayed object cannot start a second run;
                            the object's own id is scanner-object-{tenant}-{sha256(s3_key)[:32]}
                            (writers.py:137)

--- and the live proof that they hold, read at 2026-08-21T12:13:26Z
                          k                      | v 
    ---------------------------------------------+---
     assets with a duplicate unique_identifier   | 0
     tools with a duplicate (asset_id,name)      | 0
     edges with a duplicate (src,dst,type)       | 0
     skills with a duplicate content hash        | 3
     usage sessions with a duplicate external_id | 0
    (5 rows)
    

    READ THE SKILLS ROW HONESTLY, because it is not zero and the earlier capture's zero was luck.
    Three skill_md_hash values appear on more than one row. That is NOT a duplicate identity — it is the
    same SKILL.md content installed in two different directories, and the dedup key is the directory
    hash, so each pair resolves to TWO DISTINCT identities:
          md_hash      | rows | distinct_identities |       names       
    -------------------+------+---------------------+-------------------
     50fe139afe38195f… |    2 |                   2 | drive-site
     f775bd1da71b57ee… |    2 |                   2 | drive-browser
     fbb32356a59306cf… |    2 |                   2 | browser-sso-login
    (3 rows)
    
    rows == distinct_identities in every group, and the key that IS the dedup key —
    assets.unique_identifier — has ZERO duplicates. So the replay guarantee holds; what the md-hash
    column shows is content sharing, not identity collision. Reporting it rather than reporting the
    convenient zero.
