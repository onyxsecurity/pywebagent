# PRDCT-11935 at6-12 — the writer image under test was BUILT FROM THE HEAD THAT MERGES, proven byte-exact
# FILE LAST WRITTEN 2026-08-24T00:51:18Z   (round-50 evidence-honesty sweep; the previous stamp was 2026-08-21T03:52:27Z)
# THIS FILE IS NOW ACCURATE AGAINST onyx head 2b60fdc4df25188104e737e6311087681355eff8, WITH THIS CORRECTION: the image proven
#   byte-exact below was built from the SUPERSEDED branch a2486bb33f, and the two symbols §3 greps for as
#   "the round's own code fix" were REVERTED OUT OF THIS PR by the round-45 and round-46 owner rulings.
#   The byte-exactness proof itself stands; what it proves is exactness against a PRE-REVERT head.
# (original stamp line follows)
# FILE LAST WRITTEN 2026-08-21T03:52:27Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx branch a2486bb33f (full a2486bb33f835af24f5850e4f4c5eb2e8b02afb9) · captured 2026-08-21T00:46:48Z
#
# The head moved this round (the G15 fix is a real code change), so the image was rebuilt from the NEW
# head and re-proven before any capture was taken. Nothing in this round's evidence rests on an image
# whose source differs from the commit that merges.

## 1. THE BUILD — the repo's own Dockerfile, from the head tree, working tree clean
    docker build --network=host -f backend_python/Dockerfile.dev \
      --build-arg EXTRAS=temporal-worker \
      --build-arg SERVICE_CMD='.venv/bin/uvicorn endpoint_asset_ingestion.app:app --host 0.0.0.0 --port 8007 --reload' \
      -t localhost:5001/onyx/endpoint-asset-ingestion:prdct11935-r26 .
    (--network=host because BuildKit's own sandbox could not resolve files.pythonhosted.org on this
     box while the host and plain containers could — a lab network quirk, not a change to the build)
    pushed digest: sha256:17dbec7e6f2bbaebe6b78cdd2c781a6dfb4aaa18860ef15364381560273db502

## 2. THE PROOF, taken from the pushed image
    head tree : 4741 .py files      image: 4742 .py files
    content mismatches on the 4741 head files: 0
    the one extra image file is an EMPTY /app/src/__init__.py from the base layer:
      4066a4067
      > e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855  ./__init__.py

## 3. ~~THE ROUND'S OWN CODE FIX IS IN THE IMAGE~~ (grep inside the pushed image)
##    CORRECTED BY THE ROUND-50 SWEEP — BOTH SYMBOLS BELOW WERE REVERTED OUT OF THIS PR.
##    WHAT THIS SECTION ASSERTED: that the image under test carried this round's code fix, evidenced by
##    two symbols being present inside it. That grep really was run and really returned these counts —
##    against the image built from the SUPERSEDED branch a2486bb33f.
##    WHAT IS TRUE AT HEAD 2b60fdc4df25188104e737e6311087681355eff8: NEITHER SYMBOL EXISTS.
##      · `_send_lock` — the ASB send-pacing lock, DROPPED by the ROUND-46 owner ruling as superseded by
##        `main`. At head `common_core/services/queue/{consumer_pool,sqs,client}.py` are byte-identical to
##        `origin/main` and `asb.py` differs from it by exactly one addition, `_system_properties`.
##      · `_meter_session_alerts` — the session-alert metering helper in `ingestion/writers.py`, also
##        DROPPED by the ROUND-46 ruling. `writers.py` is not in this PR's changed-file list at all.
##    THE COMMANDS THAT SHOW IT:
##      $ grep -rn '_send_lock'            backend_python/src backend | wc -l     ->  0
##      $ grep -rn '_meter_session_alerts' backend_python/src backend | wc -l     ->  0
##      $ git diff 822abd2356..2b60fdc4df | grep -c '_send_lock'                  ->  0
##      $ git diff 822abd2356..2b60fdc4df | grep -c '_meter_session_alerts'       ->  0
##      $ git diff --stat origin/main -- backend_python/src/common_core/services/queue/asb.py
##        1 file changed, 25 insertions(+)     (the _system_properties addition, nothing else)
##    THE ORIGINAL GREP OUTPUT IS RETAINED VERBATIM, as the record of what that image contained:
    _send_lock in common_core/services/queue/asb.py            : 2 occurrences
    _meter_session_alerts in ingestion/writers.py              : 2 occurrences
##    ^^ TRUE OF THE a2486bb33f IMAGE. FALSE OF THE SHIPPED HEAD.

## 4. WHERE IT RAN — both transports, one image, equal concurrency
    eai-azure                  localhost:5001/onyx/endpoint-asset-ingestion:prdct11935-r26   1
    endpoint-asset-ingestion   localhost:5001/onyx/endpoint-asset-ingestion:prdct11935-r26   1
    config concurrency, equalised for the transport leg so it is a single-variable experiment:
      eai-azure: concurrency: 2
      endpoint-asset-ingestion: concurrency: 2
