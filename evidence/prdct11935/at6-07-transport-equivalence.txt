# PRDCT-11935 at6-07 — TRANSPORT EQUIVALENCE, created class, on the shipped head
# FILE LAST WRITTEN 2026-08-23T19:22:09Z  ·  this stamp is the file's own last-write time; the capture times stated below belong to the
#   measurements themselves, which is why the two can differ when a later round corrected a number.
# onyx head 2b60fdc4df25188104e737e6311087681355eff8 · writer image (BOTH lanes) localhost:5001/onyx/endpoint-asset-ingestion:r46b-head · captured 2026-08-23T19:22:09Z
#
# THE SETUP. One writer image, two transports. AWS lane = the payload through Kong -> vector's s3_sink
# -> LocalStack S3 -> the bucket notification -> SQS -> endpoint-asset-ingestion (ONYX__CLOUD unset)
# -> tenant prdct11935_ta. Azure lane = the SAME payload bytes through the SAME Kong POST, fanned by
# vector's azure_blob sink -> the real lab Blob -> Event Grid -> Service Bus -> eai-azure
# (ONYX__CLOUD=azure) -> tenant prdct11935_tb. One scan, two sinks, so the payload cannot differ.
#
# TWO INSTRUMENT FIXES over the round-39 attempt, which is why that attempt reported 43 changed lines
# and this one reports 0 — the difference is the measurement, not the code:
#   1. gen-1 is quiesced for the WHOLE three-class sequence, not just inside one leg. Round 39 unpaused
#      it between legs and 11 gen-1-created TA rows leaked into the matched and reingest-skip diffs.
#   2. both tenants are read inside ONE repeatable-read snapshot (dual-dump.sh), after a measured
#      quiet check that no new object landed for a full 45s window. Round 39 dumped TA and TB as two
#      sequential shell calls; a scan landing between them makes the halves disagree by construction.
#      Measured this round: the first matched attempt showed a 2-line hash difference on
#      'Filesystem MCP Server' at 19:08, and re-reading the SAME rows atomically once the lane went
#      quiet showed them identical — the row had simply not converged in TA yet when TB was read.

=== the leg, as it ran ===
  ### CLASS=created  mode=reset   2026-08-23T19:10:39Z
  --- S3 (gen-1's store, which it LISTS): 62 object(s) before
  --- S3 after: 0 object(s)  (0 = gen-1 starts this leg from nothing)
  --- Azure Blob (the new lane's store, which it CONSUMES EVENTS FROM): not emptied by design —
      an existing blob raises no BlobCreated event, so only the objects this leg's scan writes can
      reach the queue. The tenant DB is truncated either way, so the leg still starts from zero rows.
      both tenants truncated, gen-1's S3 store emptied
      gen-1 replicas at leg start: <none> <none> 
      SCAN START 2026-08-23T19:10:51Z
      scanner exit=0
        2026-08-23T19:12:00.918016902Z	INFO	Scan run completed	{"payloads_sent": 6, "users": 1, "agents_detected_total": 4, "mcp_servers_total": 10, "projects_total": 2, "skills_total": 261, "duration_ms": 67070, "scan_run_id": "c5b3c884-cdfc-431f-8d5f-053eafc8d7c0"}
      19:12:02 ta_assets|tb_assets|ta_conn|tb_conn = 353|353|349|349
      19:12:22 ta_assets|tb_assets|ta_conn|tb_conn = 353|353|349|349
      19:12:42 ta_assets|tb_assets|ta_conn|tb_conn = 353|353|349|349
      SETTLED
      --- waiting for the lane to go quiet before the snapshot
        quiet probe 1: TA objects 20 -> 20
    wrote /tmp/prdct11935/r48/created-ta.txt (1080 lines) and /tmp/prdct11935/r48/created-tb.txt (1080 lines) from ONE repeatable-read snapshot
      DIFF ta vs tb: 0 changed lines   (sha ta=e6dba029c180a563 tb=e6dba029c180a563)
  
      (content changed for the matched leg: new skill r48-at6-matched2-191331 on the box)
  

=== the verdict ===
    $ diff created-ta.txt created-tb.txt
    (no output — the two dumps are identical)
    changed lines : 0

    lines each    : 1080
    sha256 ta     : e6dba029c180a563a09af13ce9fc1d973c4283bf507777f43bbc296f83a2ed62
    sha256 tb     : e6dba029c180a563a09af13ce9fc1d973c4283bf507777f43bbc296f83a2ed62

=== the eleven queries that dump covers ===
    --- assets by type/sub_type/status/is_deleted
    --- assets (name|type|sub_type|unique_identifier|status|is_deleted|version|host_type|surface|app_definition_id|awaiting_discovery)
    --- mcp servers (name|uid|type|source_type|publisher|is_public|hash)
    --- mcp tools (server|tool)
    --- skills (name|md_hash|dir_hash)
    --- subagents (name|uid|md_hash|format)
    --- usage sessions (external_id|actor|email|has_content_events|account_type)
    --- scanner installations (agents|mcp|skills|version|trigger|host_type|scan_command)
    --- asset connections (type|src|dst|is_deleted|metadata)
    --- issue instances (issue_identifier|title|asset|resolve_state)
    --- mcp ingestion state
